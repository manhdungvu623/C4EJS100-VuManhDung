console.log('var in most cases, work like let, in some sense, var can be called an older version of let');
console.log('const is like let, except once its value is set, it cant be changed');
console.log('lets scope is smaller than var and can not escape a scope');
console.log('var can be understood as let in old tutorials');
console.log('let is slightly better than var in terms of clarity, code readability');
console.log('const is good because when it cant be updated you are absolutely confident about its value');
console.log('declare a value as const first, if change is necessary, change it into let later');
